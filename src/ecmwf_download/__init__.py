"""ecmwf-download: ERA5 precipitation processing for the Cauvery catchment.

The runnable pipeline lives in ``src/processing_ecmwf_dwnld/`` (each script
exposes ``main()``); this package exists so the project builds and resolves
cleanly with uv.
"""

__version__ = "0.1.0"
